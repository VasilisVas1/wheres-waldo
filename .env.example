# Copy this file to `.env` (which git ignores) and paste your own key.
# Only needed to download the annotated dataset from Roboflow (notebook 01, or
# `python -m src.data.download_dataset`). Never commit the real key.
ROBOFLOW_API_KEY=your_roboflow_api_key_here
